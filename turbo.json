{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["lint"],
      "outputs": ["dist/**"]
    },
    "format": {},
    "lint": {
      "dependsOn": ["format"]
    }
  }
}
